#include <stdio.h>

int main()
{

    // arithmetic operators = + - * / % ++ --

    int x = 2;
    int y = 3;
    int z = 0;

    // z = x + y;
    // z = x - y;
    // z = x * y;
    // z = x / y;
    // z=x % 2;

    // x+=3;
    // x-=3;
    // x*=2;
    // x/=2;
    printf("%d\n", z);
    return 0;
}